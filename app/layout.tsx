import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Cognixs.ai | Inteligência artificial para empresas",
  description:
    "Agentes, automações e produtos de inteligência artificial sob medida para transformar processos reais da sua empresa.",
  other: {
    "codex-preview": "development",
  },
  icons: {
    icon: "/cognixs-mark.png",
    shortcut: "/cognixs-mark.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
